dist: trusty
language: node_js
version: 1.0
node_js: stable

script:
- echo "skipping tests"

jobs:
  include:
    - stage: npm release
      node_js: stable
      before_deploy:
      - npm version patch
      - yarn install
      - yarn package
      deploy:
        skip_cleanup: true
        edge: true
        provider: npm
        email: "webdev@experium.ru"
        api_key: "$NPM_API_KEY"
        on:
          branch: master
